# Pre-six-fighter backup

The project state before adding JUNO and ORIN is preserved by Git commit:

`9d6aaf8 Improve mobile landscape combat visibility`

To inspect or restore individual files without discarding unrelated work:

```powershell
git show 9d6aaf8:path/to/file
```

The generated fighter artwork added after this point is stored under
`artifacts/sprites/new-fighters/` and `public/assets/`.
