# Rift Forge QA 보고서

## 1. 범위와 판정 원칙

이 문서는 현재 저장소의 자동화 범위, 수동 QA 매트릭스와 알려진 제한을 설명합니다. 전투 규칙의 단위 테스트만 통과했다고 제품 전체를 통과로 간주하지 않습니다. 릴리스 판정은 깨끗한 의존성 설치 후 전체 밸런스 보고서까지 갱신하는 `npm run verify`가 종료 코드 0으로 끝나고 실기 모바일 점검이 완료됐을 때 내립니다.

밸런스 수치는 조정 과정에서 달라질 수 있으므로 이 문서에 복사해 고정하지 않습니다. 최신 수치, 생성 시각과 판정은 [`BALANCE_REPORT.md`](./BALANCE_REPORT.md)와 로컬 생성물 `artifacts/balance-full.json`을 단일 근거로 사용합니다. JSON은 반복 실행용 산출물이므로 버전 관리에서 제외하고, 사람이 검토할 보고서만 저장소에 남깁니다.

## 2. 최종 자동 검사

2026-07-20 동일 작업트리에서 `npm run verify`를 처음부터 끝까지 연속 실행했고 종료 코드 0을 확인했습니다.

| 검사 | 결과 | 관찰 |
| --- | --- | --- |
| `npm run lint` | 통과 | ESLint 오류 0 |
| `npm run typecheck` | 통과 | 앱·Node strict TypeScript 검사 통과 |
| `npm test` | 통과 | 8개 파일, 56개 테스트 통과 |
| `npm run test:balance:full` | 통과 | 2,400경기, 하드 이상치 0, 타임아웃 0.0% |
| `npm run build` | 통과 | 프로덕션 HTML·해시 CSS/JS·서비스 워커 생성 |
| `npm run test:e2e` | 통과 | 9개 뷰포트, 24개 실행 통과·적용되지 않는 30개 의도적 스킵 |
| 프로덕션 PWA 검사 | 통과 | JS/CSS 실제 본문 선캐시와 네트워크 차단 후 재로딩 통과 |

동일 실행에서 `BALANCE_REPORT.md`와 8개 QA 스크린샷도 갱신했습니다. 자동화가 대신할 수 없는 Android/iOS 실기 항목은 6절 체크리스트와 7절 제한에 별도로 남겨 두었습니다.

## 3. 자동 테스트 매트릭스

| 영역 | 자동화 | 핵심 검증 |
| --- | --- | --- |
| 정적 품질 | ESLint, strict TypeScript | 미사용 값, 잘못된 분기·타입, 인덱스/옵셔널 속성 안전성, 앱·도구 설정 타입 |
| 전투 수학 | `tests/unit/math.test.ts` | 충격도 증가, 체중·차지·콤보 보정, 넉백/히트스턴 상한, 각도 벡터, 시드 난수 결정성 |
| 입력 | `tests/unit/input.test.ts` | 7프레임 버퍼, 눌림/해제 에지, 홀드 중 재발동 방지, 명시적 소비, 축 정규화 |
| 데이터 | `tests/unit/data.test.ts` | 캐릭터·기술과 스테이지 런타임 스키마 검증 |
| 설정/현지화 | `tests/unit/settings.test.ts` | 손상 JSON 복구, 타입 방어·범위 제한, 버전 저장 키, 번역 폴백 |
| 전투 상태 | `tests/unit/combat.test.ts` | 3 스톡/150초, 스폰 접지, 일시정지 동결, 공중 점프·코요테 타임, 회피와 반복 약화, 복귀 재사용, 발판 통과 |
| 승패 | `tests/unit/combat.test.ts` | 링아웃·재생성, 훈련 무한 스톡, 시간 판정, 연장전, 동시 마지막 스톡 KO의 순서 편향 방지, 종료 후 입력 차단 |
| 히트 판정 | `tests/unit/combat.test.ts` | 잽 캔슬 창, 히트/허트박스 분리, 중복 적중 방지, 전역 히트스톱, 무적·갑주, 공중기 착지 딜레이, 활성 구간 반사 |
| AI | `tests/unit/ai.test.ts` | 훈련 공격 주기, 장거리 투사체 조준, 반대 방향을 본 상태에서 선회 후 발사 |
| 밸런스 집계 | `tests/unit/balance-accounting.test.ts` | 미러전 양측 집계, 좌우 교대 시 캐릭터·시작 위치·피해·복귀 통계 보존 |
| 전투 시나리오 | `tests/scenarios/combat-scenarios.test.ts` | Mira/Bram 발동 차이, 체급별 넉백, Suri 투사체 상한, 착지 후 복귀 회복, 콤보 탈출, 장시간 수치 유한성 |
| 브라우저 흐름 | `tests/e2e/flow.spec.ts` | 전체 선택/대전 흐름, 서로 다른 포인터 ID의 멀티터치, `pointercancel`·강제 입력 해제 시각 복구, 일시정지·세로 회전 틱 동결, 재시작 음향 복구, 재대전·종료 |
| 설정/훈련 E2E | `tests/e2e/flow.spec.ts` | 버튼 크기·왼손·언어 영속화와 경기 중 레이블 갱신, CPU 공격 행동, 무한 HUD, 히트박스/프레임/입력 표시, 충격도 초기화와 통계를 보존하는 위치 초기화 |
| 반응형 레이아웃 | `tests/e2e/layout.spec.ts` | 메뉴가 뷰포트 안에 있고 최소 터치 크기를 지키는지, 가로 HUD와 액션 버튼이 겹치지 않는지 |
| 시각 자료 | `tests/e2e/visual.spec.ts` | 메인, 캐릭터 선택, 전투, 일시정지, 결과, 세로 회전, 320px 협폭, 데스크톱 캡처 |
| 프로덕션 PWA | `tests/pwa/offline.spec.ts` | 프로덕션 매니페스트, 아이콘, 서비스 워커 제어, 해시 JS/CSS 실제 본문 크기, 첫 방문 뒤 오프라인 재로딩 |
| 대량 밸런스 | `scripts/balance.ts` | 결정론적 AI 대전, 미러/비미러·시작 위치 교대, 승률·피해·복귀·기술 사용·타임아웃 이상치 |

## 4. Playwright 뷰포트 매트릭스

기본 E2E 설정은 Chromium, 한국어 로케일, 다크 컬러 스킴을 사용하며 한 워커에서 순차 실행합니다.

| 프로젝트 | 뷰포트 | 터치/모바일 | 목적 |
| --- | ---: | --- | --- |
| `galaxy-s23-landscape` | 780×360, DPR 3 | 예 | 핵심 전체 흐름, 멀티터치, 가로 HUD, 주 시각 캡처 |
| `galaxy-s23-portrait` | 360×780, DPR 3 | 예 | 전투 중 세로 회전 게이트와 상태 보존 |
| `narrow-320` | 320×568, DPR 2 | 예 | 최소 폭 메뉴와 전체 페이지 캡처 |
| `mobile-360x800` | 360×800, DPR 3 | 예 | 소형 세로 안드로이드 계열 |
| `mobile-393x873` | 393×873, DPR 3 | 예 | 중형 세로 모바일 |
| `mobile-412x915` | 412×915, DPR 3 | 예 | 대형 세로 모바일 |
| `mobile-844x390-landscape` | 844×390, DPR 3 | 예 | 와이드 가로 HUD·컨트롤 분리 |
| `mobile-915x412-landscape` | 915×412, DPR 3 | 예 | 대형 가로 모바일 |
| `desktop` | 1280×720, DPR 1 | 아니요 | 데스크톱 메뉴·키보드 레이아웃 |

`visual.spec.ts`는 회귀 비교용 기준 픽셀 테스트가 아니라 검토용 파일을 생성합니다. 색·간격의 미세 회귀는 사람이 이미지를 비교해야 합니다.

현재 저장된 QA 이미지:

- [Galaxy S23 가로 메인 메뉴](./artifacts/screenshots/galaxy-s23-landscape-main-menu.png)
- [캐릭터 선택](./artifacts/screenshots/character-select.png)
- [전투](./artifacts/screenshots/combat.png)
- [일시정지](./artifacts/screenshots/pause.png)
- [결과](./artifacts/screenshots/result.png)
- [Galaxy S23 세로 회전 안내](./artifacts/screenshots/galaxy-s23-portrait-rotation.png)
- [320px 협폭](./artifacts/screenshots/narrow-320.png)
- [1280×720 데스크톱](./artifacts/screenshots/desktop-1280x720.png)

## 5. 밸런스 검증 방식

### 실행

```bash
npm run test:balance
npm run test:balance:full
```

- 빠른 게이트는 1,600경기를 실행하고 `artifacts/balance-fast.json`을 만듭니다.
- 전체 게이트는 2,400경기를 실행하고 `artifacts/balance-full.json`과 [`BALANCE_REPORT.md`](./BALANCE_REPORT.md)를 갱신합니다.
- 4개 캐릭터의 미러를 포함한 조합을 순환하고, 블록마다 P1/P2 좌석을 교대합니다.
- 각 경기는 `seed = 101 + gameIndex × 7919`로 재현할 수 있습니다.
- Vector Spire, 기믹 끔, 어려움 AI, 실제 60 Hz Core를 사용합니다.
- 미러전은 캐릭터 우열 대신 P1 시작 위치 편향을 보고, 캐릭터 전체 승률에서는 미러전을 제외합니다.

### 자동 이상치 기준

스크립트의 하드 게이트는 충분한 표본이 있는 비미러 매치업이 42–58% 범위를 벗어나는지, 캐릭터 전체 승률이 46–54% 범위를 벗어나는지 확인합니다. 미러 P1 승률 42–58%, 단일 기술의 전역 사용 비중 28% 이하, 시간 종료·무승부 비율 18% 이하도 검사합니다.

복귀 성공률, 기대 역할 상성, 전체 P1 승률은 경고로 별도 기록합니다. 하드 이상치가 하나라도 있으면 스크립트 종료 코드는 실패입니다. 수치 조정 후에는 한두 관련 값만 변경하고 단위 테스트 → 빠른 게이트 → 전체 게이트 순서로 비교해야 합니다.

### 해석 제한

AI 대 AI 통계에는 정책의 거리 선택, 반응 주기와 복귀 로직 편향이 포함됩니다. 터치 숙련도, 인간의 페이크·습관 학습, 디바이스 프레임 페이싱은 반영하지 않습니다. 따라서 자동 게이트 통과는 심각한 데이터 회귀가 없다는 증거이며 인간 매치업이 해결됐다는 증명은 아닙니다.

## 6. 수동 QA 체크리스트

### 게임 흐름

- 첫 실행 온보딩 → 타이틀 → 메인 메뉴 → 캐릭터/스테이지/난이도 → 카운트다운 → 전투 → 결과가 끊기지 않는지 확인
- 같은 캐릭터 미러전, 두 스테이지, 세 난이도와 기믹 켜기/끄기를 각각 플레이
- 일시정지, 설정 열기/닫기, 재시작, 재대전, 캐릭터 선택 복귀, 메인 메뉴 종료에서 상태 누수 확인
- 타이머 종료, 일반 KO, 동시 마지막 스톡 KO, 연장전 동시 KO를 눈으로 확인

### 입력과 모바일

- 실제 Android Chrome과 iOS Safari에서 스틱+공격+점프 멀티터치, 손가락이 화면 밖으로 나간 경우와 OS 제스처 간섭 확인
- 왼손 배치, 플로팅/고정 스틱, 최소·최대 스틱/버튼 크기, 버튼 투명도를 각각 확인
- 노치·둥근 모서리·브라우저 툴바가 HUD와 컨트롤을 가리지 않는지 확인
- 전투 중 세로 전환 시 즉시 멈추고 가로 복귀 시 수동 일시정지 의도를 보존하는지 확인
- 키보드 동시 입력, Space/방향키 스크롤 방지, 실제 게임패드별 버튼 매핑 확인

### 전투 감각

- 4 캐릭터로 지상/공중 방향 일반기, 네 방향 특수기와 차지 최대치를 모두 실행
- 회피 무적과 반복 약화, 갑주, 반사, 투사체 통과가 시각적 활성 구간과 일치하는지 확인
- 공중 점프·코요테 타임·발판 통과·빠른 낙하·복귀기 1회 제한·착지 복구 확인
- 강한 타격의 히트스톱 동안 발판과 투사체까지 멈추는지 고속 촬영 또는 프레임 도구로 확인
- 화면 가장자리 카메라, 링아웃, 재생성 무적과 180% 연장전 가독성 확인

### 접근성·현지화

- 한국어/영어를 전환한 뒤 새로고침해 언어와 `html[lang]`이 유지되는지 확인
- 키보드만으로 메뉴 포커스가 보이고 모든 설정에 접근 가능한지 확인
- 스크린 리더로 버튼 이름, 토글 상태, 카운트다운과 상태 메시지를 점검
- 모션 감소를 켰을 때 CSS 애니메이션과 강한 화면 흔들림이 억제되는지 확인
- 텍스트 확대, 고대비/강제 색상, 색각 이상 시뮬레이션에서 정보가 색만으로 전달되지 않는지 확인

### 음향·성능·PWA

- 사용자 제스처 후 Web Audio가 정상 해제되고 효과음·음악 음량 0~100%가 반영되는지 확인
- Android 지원 기기에서 진동 켜기/끄기와 강한 타격 패턴을 확인하고 iOS 미지원 동작이 오류를 내지 않는지 확인
- 저사양 모바일에서 150초 경기, 다중 투사체·파티클 상황의 FPS, 발열과 메모리 증가를 관찰
- HTTPS 배포에서 설치 가능 여부, 아이콘/가로 방향, 온라인 1회 실행 후 완전 오프라인 재시작을 확인
- 새 캐시 버전 배포 뒤 열린 탭과 재실행 앱이 예상 버전으로 갱신되는지 확인

## 7. 알려진 제한과 잔여 위험

- 자동 브라우저 검사는 현재 Chromium만 사용합니다. Firefox와 WebKit/Safari 회귀는 자동으로 차단하지 않습니다.
- 브라우저 전체 경기 흐름은 Kade 대 Mira와 Vector Spire를 대표 조합으로 사용합니다. Bram·Suri와 Drift Garden은 데이터·Core·밸런스 자동화에는 포함되지만 모든 조합의 브라우저 렌더 통합 경기는 수동 매트릭스에 남아 있습니다.
- 터치 E2E는 합성 PointerEvent를 사용하므로 실제 디지타이저 지연, OS 가장자리 제스처와 제조사별 멀티터치 차이를 재현하지 못합니다.
- 진동, 소리의 체감 음량·지연, 전체 화면 승인, 게임패드 매핑은 자동 테스트가 실물 품질을 판정하지 못합니다.
- 스크린 리더, 키보드 탭 순서, 색 대비와 WCAG 적합성에 대한 전용 자동 감사는 아직 없습니다.
- 스크린샷은 생성되지만 승인된 기준 이미지와의 픽셀 차이 게이트는 없습니다.
- 장시간 성능, 메모리, 배터리와 열 스로틀링 예산을 강제하는 자동 성능 테스트는 없습니다.
- 서비스 워커 설치 자체에는 네트워크가 필요합니다. 설치가 완료되면 앱 셸과 현재 빌드 자산을 선캐시하지만, 한 번도 온라인으로 방문하지 않은 기기에서의 실행은 지원하지 않습니다.
- 캐시는 이름 기반 수동 버전 관리입니다. 릴리스에서 정적 파일이 바뀌면 캐시 이름과 업데이트 동작을 함께 검토해야 합니다.
- AI Lab은 모든 빌드에서 접근 가능하지만 Kade 대 Suri, 고정 스테이지·시드로 동작하는 진단 모드이며 일반 대전 설정을 대신하지 않습니다.
- 게임은 로컬 P1 대 CPU만 지원합니다. 로컬 2인, 네트워크 대전, 저장 동기화는 범위 밖입니다.
- 자동 밸런스 결과는 인간 플레이테스트를 대체하지 않습니다. 캐릭터별 학습 난도와 터치 실행 편차는 별도 세션에서 관찰해야 합니다.

## 8. 릴리스 인수 절차

```bash
npm ci
npm run verify
npm run screenshots
```

인수자는 다음을 확인합니다.

1. 세 명령이 모두 종료 코드 0으로 끝났습니다.
2. `BALANCE_REPORT.md`의 생성 시각이 현재 커밋 검증 시점이며 자동 이상치 게이트가 통과했습니다.
3. `artifacts/screenshots/`의 8개 핵심 화면을 열어 겹침, 잘림, 잘못된 언어와 빈 캔버스가 없습니다.
4. Android Chrome과 iOS Safari 실기에서 가로 멀티터치, 음향, 회전 보존과 PWA/홈 화면 동작을 점검했습니다.
5. 알려진 제한 중 이번 릴리스를 막는 항목이 없거나 명시적으로 승인됐습니다.
