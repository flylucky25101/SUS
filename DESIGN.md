# Rift Forge 설계 문서

## 1. 제품 방향

Rift Forge의 목표는 설치 부담 없이 짧게 즐길 수 있으면서도, 플랫폼 파이터의 핵심인 간격·방향 선택·누적 충격도·복귀 판단이 살아 있는 1 대 1 전투를 제공하는 것입니다.

핵심 설계 원칙은 다음과 같습니다.

- 모바일 우선: 가로 화면에서 두 엄지로 이동과 행동을 동시에 입력할 수 있어야 합니다.
- 읽을 수 있는 전투: 캐릭터 실루엣, 공격 궤적, 히트스톱, 충격도와 스톡을 작은 화면에서도 빠르게 구분합니다.
- 결정론적 규칙: 같은 상태·입력·시드는 같은 결과를 만들고, 렌더러 없이 대량 시뮬레이션할 수 있어야 합니다.
- 데이터 중심 밸런스: 캐릭터와 기술 수치는 한곳에 정의하고 스키마 검증과 헤드리스 대전으로 회귀를 찾습니다.
- 공정한 CPU: 난이도는 반응 주기·예측·실수·회피·복귀 정확도로 구분하며 플레이어의 미공개 입력을 읽지 않습니다.
- 오리지널 표현: 캐릭터, 스테이지, UI와 절차적 그래픽·음향을 프로젝트 내부에서 직접 구성합니다.

현재 범위는 로컬 플레이어 대 CPU입니다. 네트워크 대전, 계정, 서버 저장, 매치메이킹은 포함하지 않습니다.

## 2. 시스템 아키텍처

### 2.1 계층과 책임

| 계층 | 주요 파일 | 책임 |
| --- | --- | --- |
| Core | `src/core/*` | 60 Hz 월드 상태, 입력 버퍼, 이동·충돌·공격·투사체·KO·시간 판정, 결정론적 수학 |
| Data | `src/data/*` | 4 캐릭터 스탯/기술과 2 스테이지 정의, 런타임 스키마 검증 |
| Input | `src/input/input-manager.ts` | 키보드·멀티터치·게임패드를 공통 `InputCommand`로 변환 |
| AI | `src/ai/controller.ts` | 월드의 공개 상태를 읽고 CPU용 `InputCommand`를 생성 |
| Render | `src/render/*` | Phaser 장면, 절차적 캐릭터·스테이지·투사체·이펙트, 동적 카메라 |
| UI | `src/ui/*` | 온보딩, 메뉴, 선택 흐름, HUD·일시정지·결과·훈련 패널, 설정과 현지화 |
| Audio | `src/audio/audio.ts` | Web Audio 합성 효과음/배경 펄스와 브라우저 진동 |
| PWA | `public/*` | 매니페스트, SVG 아이콘, 프로덕션 서비스 워커와 캐시 |
| Tooling | `scripts/*`, `tests/*` | 단위·시나리오·E2E·PWA 검사와 대량 밸런스 시뮬레이션 |

의존 방향은 입력/AI → 공통 명령 → Core → 상태/이벤트 → Render/UI/Audio입니다. Core는 Phaser, DOM, AudioContext에 의존하지 않습니다. 따라서 Vitest와 Node 기반 밸런스 러너가 브라우저 없이 같은 전투 규칙을 실행합니다.

### 2.2 “순수 60 Hz 코어”의 의미

Core는 외부 I/O와 렌더링에서 분리된 순수 도메인 경계입니다. `stepWorld(state, commands)`는 난수나 벽시계, DOM을 직접 읽지 않으며 입력으로 받은 상태와 명령만 사용합니다. 성능을 위해 전달된 `WorldState`를 제자리에서 갱신하므로 함수형 프로그래밍 의미의 불변 함수는 아니지만, 같은 초기 상태·명령열에는 결정론적 결과를 냅니다.

`ArenaScene`은 렌더 프레임의 `delta`를 누산하고 `1000 / 60`ms마다 Core를 한 번 진행합니다. 긴 프레임에서는 최대 5개의 보정 스텝만 처리하고 과도한 누산은 버려 입력 폭주를 막습니다. 디버그 배속에서는 같은 고정 스텝을 더 많이 실행할 뿐 전투 규칙의 시간 단위는 계속 프레임입니다.

한 전투 스텝의 주요 순서는 다음과 같습니다.

1. P1/P2 명령을 유한한 `[-1, 1]` 축과 불리언 버튼으로 정규화합니다.
2. 기존 히트스톱이 있으면 입력 에지만 갱신하고 캐릭터·투사체·타이머·스테이지 모션을 멈춥니다.
3. 상태 타이머, 입력 버퍼와 행동, 공격 프레임을 갱신합니다.
4. 근접 히트 후보를 모아 우선순위 또는 상호 타격을 한꺼번에 판정합니다.
5. 새 히트스톱이 시작되지 않았다면 투사체, 스테이지 모션과 물리를 진행합니다.
6. 장외 대상을 스냅샷으로 모아 동시 KO를 공정하게 처리한 뒤 경기 타이머를 갱신합니다.
7. 같은 틱에서 발생한 전투 이벤트에 틱 번호를 기록해 렌더·음향·로그로 전달합니다.

### 2.3 상태와 이벤트

`WorldState`는 시간, 연장전, 일시정지, 경기 결과, 두 파이터, 투사체, 훈련 설정을 소유합니다. `FighterState`에는 위치·속도·방향, 충격도·스톡, 접지·점프·복귀, 공격 프레임, 무적·갑주·회피 쿨다운, 입력 메모리와 텔레메트리가 들어 있습니다.

Core는 `attack-start`, `hit`, `strong-hit`, `jump`, `land`, `dodge`, `projectile`, `ringout`, `respawn`, `match-end` 이벤트를 만듭니다. 렌더러와 오디오는 이 이벤트를 소비하지만 판정 결과를 다시 바꾸지 않습니다. 이 단방향 구조 덕분에 화면 흔들림이나 파티클을 꺼도 전투 결과가 달라지지 않습니다.

## 3. 전투 설계

### 3.1 승패와 스톡

- 빠른 대전은 각 3 스톡, 150초로 시작합니다.
- 블래스트 존을 벗어나면 스톡이 하나 줄고, 남은 스톡이 있으면 짧은 지연과 무적을 거쳐 지정 위치에 재생성됩니다.
- 시간이 끝나면 스톡이 많은 쪽, 스톡이 같으면 충격도가 낮은 쪽이 승리합니다.
- 스톡과 충격도까지 같으면 두 선수를 1 스톡·180%로 재생성해 18초 연장전에 들어갑니다.
- 일반 경기에서 마지막 스톡을 동시에 잃으면 배열 순서로 승자를 고르지 않고 연장전으로 전환합니다.
- 연장전에서 동시에 KO되거나 연장전 종료 시 스톡·충격도가 같으면 무승부입니다.
- 훈련 모드는 스톡과 타이머를 소모하지 않고 장외 캐릭터를 계속 재생성합니다.

### 3.2 충격도와 넉백

공격은 체력을 깎아 0으로 만드는 대신 충격도 퍼센트를 누적합니다. 넉백은 기술의 기본 넉백·성장률, 누적 충격도, 방어자의 체중, 차지 비율과 긴 콤보 탈출 보정을 사용합니다. 충격도가 높고 체중이 가벼울수록 멀리 날아갑니다.

방어자는 피격 순간 스틱으로 수평·수직 발사 방향에 제한적인 영향을 줄 수 있습니다. 극단값은 전역 속도·넉백·히트스턴 상한으로 제한하고, 비정상 숫자는 입력 정규화와 수학 함수에서 방어합니다.

### 3.3 방향 공격과 차지

공통 행동 버튼은 일반 공격과 특수 공격 두 개지만 스틱 방향으로 기술이 달라집니다.

- 중립 일반: 지상 잽. 일부 잽은 회복 구간에서 새 일반 입력으로 연계할 수 있습니다.
- 옆·위·아래 일반: 각각 수평 견제, 대공, 낮은 자세/지면 압박 역할을 가집니다.
- 공중 일반: 공중에서는 방향과 무관하게 캐릭터별 공중 전용 일반기를 사용합니다.
- 중립·옆·위·아래 특수: 투사체, 이동기, 복귀기, 반사·갑주 등 캐릭터 역할을 강화합니다.
- 지상 일반 버튼은 누른 동안 최대 36프레임 차지하고, 놓거나 최대치에 도달하면 선택된 방향 기술을 발동합니다.

각 기술은 스타트업·활성·회복·착지 딜레이, 피해, 넉백, 각도, 히트스턴·히트스톱, 히트박스, 이동 충격, 쿨다운, 갑주·무적·투사체 속성을 데이터로 가집니다. 반사와 투사체 통과는 해당 기술의 활성 구간에서만 적용됩니다.

### 3.4 이동, 회피와 복귀

- 지상 가속/마찰과 공중 가속/마찰을 분리해 캐릭터별 기동성 차이를 만듭니다.
- 기본 점프 뒤 공중 점프를 한 번 더 쓸 수 있으며, 발판을 떠난 뒤 5프레임 코요테 타임과 7프레임 입력 버퍼가 있습니다.
- 아래 입력으로 단방향 발판을 통과하고, 공중 아래 입력은 빠른 낙하를 유도합니다.
- 회피는 짧은 이동과 무적을 주지만 쿨다운이 있습니다. 짧은 간격으로 반복하면 지속 경직과 쿨다운이 늘고 무적·이동 성능이 약해집니다.
- `위 + 특수` 복귀기는 공중 체류 중 한 번만 허용됩니다. 착지하면 사용권이 돌아오며, 복귀 시도·성공은 밸런스 텔레메트리에 기록됩니다.
- 공중 공격 도중 착지하면 공격을 취소하고 기술별 착지 딜레이를 정확히 적용합니다.

### 3.5 히트스톱, 갑주와 동시 판정

피격 시 공격자와 방어자 모두 기술별 히트스톱을 받습니다. 어느 한쪽이라도 히트스톱이면 두 파이터뿐 아니라 투사체의 위치·수명, 경기 타이머와 이동 발판의 모션 틱도 함께 멈춥니다. 화면 효과가 아니라 전역 전투 시간의 정지로 취급합니다.

갑주는 피해는 받되 남은 갑주 프레임 동안 발사와 피격 상태 전환을 막습니다. 무적은 피해 자체를 무시합니다. 양쪽 활성 히트박스가 같은 틱에 겹치면 기술의 피해·범위·갑주·스타트업에서 계산한 우선순위 차이가 충분할 때 강한 쪽만 적중하고, 차이가 작으면 상호 타격이 가능합니다.

## 4. 캐릭터와 매치업 의도

| 역할 | 캐릭터 | 설계 의도 |
| --- | --- | --- |
| Vanguard | Kade | 전 영역에 답이 있는 기준형. 투사체 반사와 안정적 복귀로 제어형에 대응합니다. |
| Rush | Mira | 빠른 스타트업과 이동으로 준비 시간이 긴 기술을 압박하지만 가벼운 체중으로 위험을 부담합니다. |
| Tank | Bram | 갑주와 큰 단발 화력으로 근접 교환을 이기지만 이동·복귀와 기술 회복이 느립니다. |
| Control | Suri | 투사체와 긴 사거리로 공간을 닫지만 근접 압박과 투사체 대응에 취약하도록 설계합니다. |

AI 정책은 이 역할을 그대로 사용해 접근·후퇴 거리, 일반/특수 선택과 투사체 대응을 달리합니다. 쉬움·보통·어려움은 24/12/6프레임 기준 반응 주기와 서로 다른 실수율·예측 프레임·회피·복귀 정확도를 사용합니다. 입력은 사람과 같은 `InputCommand` 파이프라인을 지나므로 AI만 사용할 수 있는 이동이나 공격은 없습니다.

AI 대 AI 통계는 데이터와 정책의 큰 이상을 찾는 회귀 도구이지 인간 대전의 최종 밸런스 판정은 아닙니다. 현재 생성 보고서와 한계는 [`BALANCE_REPORT.md`](./BALANCE_REPORT.md)를 따릅니다.

## 5. 스테이지 설계

Vector Spire는 좌우 대칭 배치와 고정 보조 발판으로 시작 위치·카메라·AI 정책을 비교하기 위한 기준 스테이지입니다. 헤드리스 밸런스 러너는 이 스테이지에서 기믹을 끄고 실행합니다.

Drift Garden은 수직 이동 발판 두 개와 수평 이동 발판 한 개로 위치 변화에 적응하도록 만듭니다. 발판 이동은 `motionTick` 기반 주기 함수로 계산되므로 결정론적이며, 기믹 설정을 끄거나 히트스톱 중일 때는 이동하지 않습니다. 접지한 캐릭터는 발판의 프레임 간 변위를 함께 전달받습니다.

## 6. 입력과 모바일 UX

`InputManager`는 터치 축을 우선하고, 터치가 데드존 안이면 키보드, 그다음 게임패드 축을 사용합니다. 행동 버튼은 세 입력 소스의 논리합입니다. 입력 에지를 7프레임 버퍼에 기록해 짧은 터치도 전투 상태가 허용되는 순간 소비할 수 있습니다.

터치 버튼은 포인터 ID별로 추적하므로 스틱 이동, 공격, 점프를 동시에 누를 수 있습니다. `pointercancel`과 캡처 상실에서도 입력을 해제해 버튼 고착을 방지합니다. 가상 스틱에는 18% 데드존과 반경 정규화가 적용됩니다.

세로 화면으로 회전하면 전투 상태를 보존한 채 자동 일시정지하고 가로 회전 안내를 표시합니다. 가로로 돌아오면 사용자가 직접 일시정지한 상태가 아닌 경우에만 재개합니다. 브라우저 탭이 숨겨져도 자동 일시정지합니다.

## 7. 렌더링과 피드백

Phaser는 `RESIZE` 스케일 모드와 기기 픽셀 비율 상한 2를 사용합니다. 캐릭터, 스테이지, 투사체와 파티클은 런타임 그래픽으로 그리며, 두 캐릭터의 거리에 따라 카메라 중심과 줌을 보간합니다.

Core 이벤트는 다음과 같은 비판정 피드백을 만듭니다.

- 일반/강한 피격 파티클과 기술별 합성음
- 강한 피격 진동과 선택 가능한 화면 흔들림
- 점프·착지 먼지, 회피 잔상, 링아웃·재생성 효과
- 훈련 히트박스/허트박스와 프레임·입력 정보
- AI Lab의 FPS, 틱, 엔티티·효과·적중 지표와 이벤트 로그

모션 감소를 켜면 CSS 애니메이션과 강한 화면 흔들림을 억제합니다. 효과 설정은 Core에 영향을 주지 않습니다.

## 8. 접근성·현지화·설정

- 한국어와 영어 문자열을 한 타입 안전한 사전에 두고, 브라우저 언어가 한국어가 아니면 영어를 기본으로 선택합니다.
- 선택 언어는 `document.documentElement.lang`과 버전이 있는 `localStorage` 설정에 반영합니다.
- 버튼과 터치 컨트롤에 의미 있는 레이블, 선택 상태에 `aria-pressed`, 토글에 `role="switch"`와 `aria-checked`, 카운트다운·토스트에 live region을 사용합니다.
- 키보드 포커스 표시, 최소 터치 크기, 화면 노치용 `safe-area-inset-*`, 가변 스틱/버튼 크기와 투명도를 제공합니다.
- 왼손 배치, 플로팅 스틱, 모션 감소, 화면 흔들림, 진동, 효과음·음악 음량과 전체 화면을 설정할 수 있습니다.
- 설정 값은 파싱 시 타입을 확인하고 허용 범위로 제한하며, 손상된 저장 값은 기본값으로 복구합니다.

이 기능들은 접근성의 기반이지만 WCAG 적합성 선언을 뜻하지는 않습니다. 스크린 리더 운용, 색 대비, 스위치 컨트롤과 실제 저사양 기기 테스트는 별도 수동 QA 대상입니다.

## 9. PWA와 오프라인 모델

프로덕션에서만 `sw.js`를 등록합니다. 설치 단계에는 HTTP 캐시와 분리된 새 요청으로 앱 셸, 매니페스트와 아이콘을 선캐시하고, 빌드된 `index.html`의 `src`/`href`를 읽어 해시된 JavaScript와 CSS의 실제 본문도 같은 설치 수명 안에 저장합니다. 문서 탐색은 온라인에서 최신 `index.html`을 우선 받아 캐시를 갱신하고 오프라인일 때 저장본으로 폴백합니다. 그 밖의 같은 출처 GET 자산은 `Vary` 헤더 차이에 안전한 캐시 우선 조회로 제공하면서 새 네트워크 응답을 런타임 캐시에 저장합니다.

캐시 이름은 버전 문자열로 관리하고 활성화 때 이전 캐시를 제거합니다. 새 버전에서 캐시 이름을 올리는 일은 릴리스 절차의 일부입니다. 최초 온라인 방문 전의 완전한 오프라인 설치, 다른 출처 리소스, 서버 동기화는 지원 범위가 아닙니다.

## 10. 검증 전략

- 수학·입력·설정·데이터·AI·전투 규칙은 Vitest로 브라우저 없이 검증합니다.
- 캐릭터 체급, 스타트업, 투사체 제한, 복귀 초기화와 수치 안정성은 시나리오 테스트로 묶습니다.
- Playwright는 모바일 멀티터치, 일시정지·회전 동결, 결과/재대전, 설정 영속화·언어, 훈련 도구와 다양한 뷰포트 레이아웃을 확인합니다.
- 별도 프로덕션 Playwright 설정은 매니페스트, 서비스 워커 제어, 해시 자산의 캐시 본문 크기와 오프라인 재로딩을 확인합니다.
- 밸런스 러너는 동일 Core와 어려움 AI로 미러를 포함한 조합을 좌우 교대 실행하고, 매치업·캐릭터·기술 사용·복귀·타임아웃 이상치를 판정합니다.

구체적인 실행 명령, 자동화 범위와 남은 수동 검증 항목은 [`QA_REPORT.md`](./QA_REPORT.md)에 정리되어 있습니다.
